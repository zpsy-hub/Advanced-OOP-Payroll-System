if (Test-Path build) { Remove-Item -Recurse -Force build }; if (Test-Path sources.txt) { Remove-Item sources.txt }; New-Item -ItemType Directory -Force build/classes | Out-Null; Get-ChildItem -Recurse src\*.java | Where-Object { $_.FullName -notlike '*\test\*' } | Select-Object -ExpandProperty FullName | Out-File sources.txt -Encoding ASCII; $count = (Get-Content sources.txt).Count; Write-Host "Found $count Java files"; javac -encoding UTF-8 -cp "lib/*" -d build/classes -sourcepath src "@sources.txt"
